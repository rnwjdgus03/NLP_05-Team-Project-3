# 홀드아웃6 결과 — 원본 풀 251~300번

실행일: 2026-08-07

입력: `data/holdout6_articles.csv` 50건

GPU 반환 ZIP SHA-256: `01ba3a26554106137d9180568cbc7bda7136f70a254cbaf2ca4db3b097a24eea`

## 결론

**물가지표 의미 게이트와 mapping type 복구는 사전등록 조건을 통과했지만, 전체 안전성 판정은 실패했다.**

- 수입·수출·생산자·소비자 물가지표 계열 충돌 후보의 `READY`: **0건**
- target-bearing 주장을 집계 OBJ로 자동 확정한 건: **0건**
- mapping `READY`에서 빈 `mapping_type`: **0건**
- KOSIS `API_ERROR`: **0건**
- 평가 집합 60건 중 mapping `READY`: **3건 (5.0%)**
- 실제값 판정 `불일치`: **2건**
- 수동 검수 결과 거짓 `불일치`: **2건**
- 사용할 수 있는 실제값 확정: **0건**

두 불일치는 모두 `2025년 1월 1~20일` 누계 주장을 `2025년 1월` 전체 월간값과 비교한 기간 해상도 오류다. 따라서 사전등록의 “거짓 불일치 0건” 목표를 통과하지 못했다. 확정률은 홀드아웃5의 1/60에서 3/60으로 올랐지만, 세 건 모두 그대로 사용할 수 없어 성능 향상으로 해석하지 않는다.

## 실행 무결성

| 항목 | 결과 |
|---|---:|
| 기사 / 고유 H6 기사 ID | 50 / 50 |
| KSS 문장 | 793 |
| HCX 주장 문맥 | 198 |
| HCX measurement | 398 |
| mapping READY 입력 / 잠근 평가 집합 | 60 / 60 |
| BGE-M3 표 인덱스 | 107,138표, 1,024차원 |
| Chroma 좌표 인덱스 | 51,205좌표, 1,024차원 |
| validate 및 실제값 검증 API_ERROR | 0 |

고정 입력과 Colab 번들 해시는 사전등록값과 일치했다.

- 홀드아웃6 입력: `ee33db5bbc17760f588c725127f46f86cc8393e00167fa9da6b15c5b2bab8bb7`
- Colab 입력 번들: `863e38eb9e5a00ebd1b0591ebc99a867a9bd3b05634f8628e4b6c04128f98866`

## 사전등록 판정

### 1. 물가지표 의미 충돌 READY 0건 — 통과

수입·수출·생산자·소비자 물가지표 계열에서 주장과 표가 충돌한 `READY`는 없었다. 홀드아웃5의 `수입물가 → 소비자물가` 오류는 재발하지 않았다. 전체 후보에서는 `INDICATOR_TABLE_MISMATCH` 6행이 보류됐다.

### 2. target-bearing aggregate READY 0건 — 통과

세 `READY` 모두 `claim_target_terms`가 비어 있었다. target-bearing 주장을 집계 OBJ로 자동 확정한 건은 0건이다.

### 3. 비교 기준이 다른 직접 비율 ITEM READY 0건 — 통과

`READY`의 mapping type은 `direct` 1건과 `rate_from_level` 2건이었다. 직접 비율 ITEM을 잘못 확정한 증감률 행은 없었다.

### 4. 구조적으로 판정 가능한 증감률의 빈 mapping type 0건 — 통과

`READY`에서 빈 mapping type은 없었다. `value_type=증감률`이고 `change_base`가 전월·전년동월인 후보 중 mapping type이 빈 68행은 모두 KOSIS 단위가 `-` 또는 `십만명당`이라 증감률 계산 차원을 판정할 수 없었고, 전부 `MAPPING_FAILED` 또는 `NEEDS_CONFIRMATION`으로 남았다.

### 5. 거짓 불일치 0건 — 실패

`H60017-SPC8813B67E1-m1`은 1월 1~20일 수출액 316억 달러를 1월 전체 수출액 491.77억 달러와 비교했다. `measurement_period=20250101~20250120`이 `period=202501`, `prd_se=M`으로 축약되고 부분 월 구간 필드가 비어 있어 월 전체 좌표가 `READY`가 됐다.

`H60017-SPC8813B67E1-m2`도 같은 부분 월 수출 증감률 약 -5%를 1월 전체 전년동월비 -10.11%와 비교했다. 두 건 모두 기사 오류가 아니라 비교 기간 해상도 불일치다.

### 6. API 오류 0건 — 통과

validate와 실제값 검증의 API 오류 합계는 0이다. 실패는 네트워크가 아니라 기간·의미·다축 조회 로직에서 발생했다.

## 세 번째 READY의 문제

`H60017-SP38778E3EA3-m1`은 전국 수출 흐름의 `2023년 9월(-4.4%)`을 말하지만 다음처럼 잘못 구조화·매핑됐다.

1. 월간 `202309`가 아니라 연간 `2023`, `prd_se=Y`로 추출됐다.
2. 전국 통관 수출이 `우주산업및항공제조산업실태조사 > 국가별 수출액-전체` 표로 매핑됐다.
3. 검증된 좌표에는 `objL1=합계`, `objL2=전체`가 있었지만 실제값 조회는 OBJ_L2를 전달하지 않아 `ACTUAL_DERIVATION_FAILED`가 됐다.

즉 값 판정은 차단됐지만, 산업 범위가 다른 표가 `READY`가 된 것 자체가 의미 게이트의 일반화 부족이다.

## 커버리지

| 버킷 | 홀드아웃5 | 홀드아웃6 |
|---|---:|---:|
| `COORDINATE_NOT_FOUND` | 18 | 20 |
| `UNIT_UNVERIFIABLE` | 17 | 11 |
| `CANDIDATE_NOT_DECISIVE` | 12 | 8 |
| `COORDINATE_RETURNS_NOTHING` | 4 | 10 |
| `NO_DATA_AT_COORDINATE` | 7 | 4 |
| `PERIOD_MISSING` | 0 | 4 |
| `PERIOD_AFTER_ARTICLE` | 1 | 0 |
| mapping `CONFIRMED` | 1 | 3 |

그룹 기준으로는 시스템 한계 49건, 확인 불가 8건, mapping 확정 3건이다. 다만 mapping 확정 세 건 중 실제값을 안전하게 확정한 건은 0건이다.

## 다음 개발 순서

홀드아웃6 결과에 맞춰 임계값을 조정하거나 이 묶음을 다시 성능 평가에 사용하지 않는다. 저장된 세 오류 행은 개발 회귀 사례로만 사용하고, 변경 평가는 미사용 풀 301~350번에서 수행한다.

1. 일부분 기간(`1~20일`, `상반월`, `첫 N일`)을 월 전체와 비교하지 않는 `PARTIAL_PERIOD_UNSUPPORTED` 게이트를 추가한다.
2. 명시적 `YYYY년 M월`이 과거 비교 시점이어도 `YYYYMM`과 월 주기를 보존한다.
3. 일반 수출·수입 주장과 특정 산업 실태조사 표의 범위 충돌을 category·기관·조사 모집단으로 차단한다.
4. 실제값 조회에 검증된 OBJ_L2~OBJ_L8을 모두 전달한다.
5. 위 오류를 200건 개발 회귀와 저장 회귀 테스트에 추가한 뒤 홀드아웃7을 사전등록한다.
