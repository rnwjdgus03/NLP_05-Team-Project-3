# Frontend connection report

## Current topology

```text
Browser
  → KOSIS frontend/BFF (127.0.0.1:3100)
    → v31b FastAPI (127.0.0.1:8000)
      → HCX + BGE/Reranker + PostgreSQL + KOSIS Open API
```

The browser calls only `/api/verifications`. The BFF injects `KOSIS_SERVICE_API_KEY` on the server, so no HCX, KOSIS, or service API key is included in frontend JavaScript.

## Verified state

- `kosis-frontend.service`: active
- Frontend health: `ready`
- Internal API connection: true
- Static form and result UI: HTTP 200
- BFF result proxy: returned the real-article E2E job generated by frozen `v31b_20260821_r1`
- Existing KOSIS MCP preserved on port 3000
- Frontend assigned to port 3100

## Development access

```powershell
ssh -N -L 3100:127.0.0.1:3100 `
  -i "C:\Users\김진성\.ssh\3rd.pem" `
  ubuntu@SERVER_IP
```

Open `http://127.0.0.1:3100` after the tunnel connects.

## Before public launch

Keep the server bound to localhost until a domain, HTTPS, user authentication, request rate limiting, and an allowed-origin policy are configured. The current interface is an integration UI that the frontend teammate can replace while preserving the `/api/verifications` contract.
