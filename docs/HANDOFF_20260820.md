# 작업 인수인계 — v24b 이후

## 체크포인트

- 대상 브랜치: `Poc`
- 최신 통합 기준일: 2026-08-20
- 기준 코드: v24b 기간 보존 + Stage B 표별 좌표 슬롯
- 통합 회귀 테스트: 230 passed
- 과거 상세 이력: `codex/repro-baseline-20260727`

이 정리본은 과거 실행 산출물과 구버전을 제거하고 현재 실행 경로, 잠금 골드, 평가 근거만 남긴다.

## 완료된 작업

1. 기사 문맥 보존 전처리와 HCX claim/measurement 추출
2. `in_ready` 1차 규칙 게이트와 검색 후 2차 READY 분리
3. 의미 불일치, 기관·통계 분야·표명, ITEM·OBJ, 단위·기간 안전 게이트
4. lexical + BGE-M3 + reranker 표 검색
5. PostgreSQL exact metadata 기반 ITEM/OBJ 좌표 조합
6. MCP/KOSIS API 조회와 확정 보류 정책
7. 기간 보존과 Stage B 표별 검토 슬롯 v24 A/B
8. v24b 동결 후 공식 보도자료 31건 블라인드 평가

## 잠금 결과

개발 좌표 골드 30건:

- Full Top-1: 15/30
- Full Top-3: 20/30
- Full Top-5: 22/30

블라인드 31건:

- 표 Top-3: 16/31
- Full Top-3/5/10: 8/31
- 최종 확정 판정: 0/31

블라인드 실패를 숨기지 않는다. 개발셋 개선과 신규 기사 일반화는 별개다.

## 다음 작업 순서

1. `_period_in_range`가 `YYYY.MM`, `YYYY-MM`, `YYYYMM`을 동일 월로 정규화하도록 수정하고 단위 테스트를 먼저 추가한다.
2. Stage A에 대표 ITEM 이름의 제한적 lexical 역색인 채널을 추가한다.
3. claim에 세부 대상이 없을 때만 공식 `계·전체·총지수` OBJ를 보존한다.
4. `data/gold/stratified_actual_coordinate_gold_v21.csv`에서 회귀를 확인한다.
5. `evaluation/blind_v24b/` 31건은 감사셋으로만 보고 튜닝하지 않는다.
6. 코드를 다시 동결한 뒤 새로운 기사와 가급적 새로운 표군으로 두 번째 블라인드셋을 만든다.

## 재개 전 확인

```powershell
git status --short --branch
python -m pytest -q
```

로컬에 있어야 하지만 Git에는 없어야 하는 자산:

- `data/indexes/kosis_bge_m3/`
- KOSIS 전체 표 카탈로그 CSV
- PostgreSQL 메타데이터 DB 또는 복원 가능한 snapshot
- `.env`
- 원시 실행 결과와 API cache

## 금지 사항

- API가 응답했다는 이유만으로 READY 또는 정답 처리하지 않는다.
- 표 의미·기관·통계 분야가 다르면 실제 값이 비슷해도 확정하지 않는다.
- 기존 블라인드 31건을 반복 튜닝해 성능 향상으로 보고하지 않는다.
- 인덱스, DB, 비밀키, 원시 tar/zip, 대량 중간 산출물을 Git에 커밋하지 않는다.
